[BITS 16]
[ORG 0x0000]

	MOV AH, 0x05
	MOV BX, BUFFER
	MOV SI, FILENAME
	MOV CX, 1000
	MOV DX, 0
	MOV DI, 0
	INT 0x80

	MOV AH, 0x00
	INT 0x80

FILENAME: DB "ABC.BIN", 0x00
BUFFER:
	MOV AH, 0x0E
	MOV AL, 'A'
	INT 0x10

	MOV AH, 0x00
	INT 0x80
BUFFER_END:
