[BITS 16]
[ORG 0x0000]

	MOV AH, 0x04
	MOV BX, BUFFER
	MOV CX, 10
	MOV DX, 3
	MOV SI, FILENAME
	INT 0x80

	MOV AH, 0x01
	MOV SI, BUFFER + 512 * 4
	MOV CX, 512
	INT 0x80

	XOR AH, AH
	INT 0x80

FILENAME: DB "TEST.TXT", 0x00
BUFFER:
