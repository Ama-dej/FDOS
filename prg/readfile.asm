[BITS 16]
[ORG 0x0000]

	MOV AH, 0x04
	MOV BX, BUFFER
	MOV CX, 512 * 3
	XOR DI, DI
	MOV DX, 0
	MOV SI, FILENAME
	INT 0x80

	MOV AH, 0x01
	MOV SI, BUFFER
	MOV CX, 512 * 3
	INT 0x80

	XOR AH, AH
	INT 0x80

FILENAME: DB "ABC.BIN", 0x00
BUFFER:	TIMES 32 DB 0
HELLO: DB "POMEMBNI PODATKI", 0x00
