PRINTH_INT:
	MOV CX, 4

.PRINT_LOOP:
	; ROL DX, 4
	ROL DX, 1
	ROL DX, 1
	ROL DX, 1
	ROL DX, 1

	MOV AL, DL
	AND AL, 0x0F

	CMP AL, 10
	SBB AL, 0x69
	DAS

	MOV AH, 0x0E
	MOV BX, 7
	INT 0x10

	LOOP .PRINT_LOOP

	JMP RET_INT
