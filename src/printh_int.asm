PRINTH_INT:
	MOV AH, 0x0E
	MOV CX, 4

.PRINT_LOOP:
	; ROL DX, 4
	ROL DX, 1
	ROL DX, 1
	ROL DX, 1
	ROL DX, 1

	MOV AL, DL
	AND AL, 0x0F

	CMP AL, 10
	SBB AL, 0x69
	DAS

	XOR BL, BL
	INT 0x10

	LOOP .PRINT_LOOP

	JMP RET_INT
